{
  "name": "stake-dapp-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ stake-dapp-subgraph",
    "create-local": "graph create --node http://localhost:8020/ stake-dapp-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ stake-dapp-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 stake-dapp-subgraph",
    "test": "graph test"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "^0.38.1",
    "matchstick-as": "0.6.0",
    "undici": "^7.16.0"
  }
}
